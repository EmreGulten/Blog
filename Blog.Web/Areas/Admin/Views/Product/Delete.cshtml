@model List<ProductDto>

<div class="container-xxl flex-grow-1 container-p-y">
    <h4 class="fw-bold py-3 mb-4">
        <span class="text-muted fw-light">Ürün Yönetimi /</span> Ürünler
    </h4>

    <!-- Basic Bootstrap Table -->
    <div class="card">
        <div class="table-responsive text-nowrap m-5">

            <table class="table table-bordered" id="productsTable" width="100%" cellspacing="0">
                <thead>
                    <tr>
                        <th>İsim</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Oluşturan</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </thead>
                <tfoot>
                    <tr>
                        <th>İsim</th>
                        <th>Oluşturulma Tarihi</th>
                        <th>Oluşturan</th>
                        <th>Durum</th>
                        <th>İşlemler</th>
                    </tr>
                </tfoot>
                <tbody class="table-border-bottom-0">
                    @foreach (var product in Model)
                    {
                        <tr name="@product.Id">
                            <td>@product.Name</td>
                            <td>@product.CreatedDate.ToShortDateString()</td>
                            <td>@product.CreatedBy</td>
                            <td>@(product.IsDeleted ? "Aktif Değil" : "Aktif")</td>
                            <td>
                                <a class="btn btn-warning btn-sm" data-id="@product.Id" asp-area="Admin" asp-controller="Product" asp-action="UndoDelete" asp-route-productId="@product.Id"><i class="bx bx-undo"></i></a>
                                @*<a class="btn btn-danger btn-sm" data-id="@product.Id" asp-area="Admin" asp-controller="product" asp-action="Delete" asp-route-productId="@product.Id"><i class="bx bx-minus-circle"></i></a>*@

                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>

@section Scripts{
    <script src="~/js/productIndex.js" type="application/ecmascript"></script>
}